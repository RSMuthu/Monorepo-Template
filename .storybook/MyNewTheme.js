import { create } from '@storybook/theming'

export default create({
  base: 'dark',
  brandTitle: 'My New Storybook',
  // we can add our own brand image & our customised UI Theme
  brandTarget: '_self',
})
