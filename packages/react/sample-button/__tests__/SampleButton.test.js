'use strict'

import SampleButton from '../src/SampleButton'
import { render } from '@testing-library/react'

describe('@rsmk/sample-button', () => {
  it('needs tests', () => {
    // TODO
    render(<SampleButton />)
  })
})
